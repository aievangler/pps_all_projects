# PPS All Projects

This repo stores all PPS projects under `projects/<project_slug>/`.

Quick commands

Create a new project from the template:

```bash
./scripts/new_project.sh <project_slug>
```

Commit and push PPS changes:

```bash
./scripts/commit_push.sh "Update PPS state"
```
